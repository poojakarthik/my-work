
// Arguments
var	ARGUMENTS	= _parseArguments(process.argv, {
	'flex-config'	: {
		'short'		: 'c',
		'values'	: ['sPath']
	},
	'list-mode'	: {
		'short'		: 'l'
	}
});

// TODO: Move this into a module (fw/cli/arguments)
var	_parseArguments	= function (aArguments, oConfig) {
	var	oArguments	= {};
	
	
	
	return oArguments;
};
