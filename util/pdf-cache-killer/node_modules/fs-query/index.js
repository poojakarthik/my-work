
var	fs	= require('fs');

/* Query Strings look like this:

	They look like regular paths, but with special sauce inside them (XPath-like):
	
		
	
	Find 

 */

var	_undefined;

var	_tokenisePath	= function (sPath) {
		
	},
	_query	= function (sPath) {

	};

// Public API
module.exports	= {
	query	: function (mPath, oOptions, fnCallback) {
		// Accept a single path, or array of paths
		var	aPaths	= Array.isArray(mPath) ? mPath : [mPath];

		// oOptions is optional
		if (typeof oOptions === 'function') {
			fnCallback	= oOptions;
			oOptions	= {};
		}

		// Process each query
		var	aResults = [];
		aPaths.forEach(function (mItem, iIndex) {
			
		});
	}
};
